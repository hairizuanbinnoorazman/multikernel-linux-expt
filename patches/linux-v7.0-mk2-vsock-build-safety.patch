diff --git a/net/vmw_vsock/mk_transport.c b/net/vmw_vsock/mk_transport.c
index 534b29af62c5..18ed747ff00a 100644
--- a/net/vmw_vsock/mk_transport.c
+++ b/net/vmw_vsock/mk_transport.c
@@ -283,6 +282,7 @@ static void mk_vsock_ipi_handler(u32 msg_type, u32 subtype,
 				 void *payload, u32 payload_len, void *ctx)
 {
 	struct sk_buff *skb;
+	u32 data_len;
 	struct virtio_vsock_hdr *hdr;
 
 	if (msg_type != MK_MSG_NETWORK || subtype != MK_NET_VSOCK_PKT)
@@ -293,6 +293,16 @@ static void mk_vsock_ipi_handler(u32 msg_type, u32 subtype,
 		return;
 	}
 
+	hdr = payload;
+	data_len = le32_to_cpu(hdr->len);
+	if (payload_len > MK_VSOCK_MAX_PKT_SIZE ||
+	    data_len > MK_VSOCK_MAX_PKT_SIZE - sizeof(*hdr) ||
+	    payload_len != sizeof(*hdr) + data_len) {
+		pr_warn("mk_vsock: invalid packet length payload=%u data=%u\n",
+			payload_len, data_len);
+		return;
+	}
+
 	skb = alloc_skb(payload_len, GFP_ATOMIC);
 	if (!skb) {
 		pr_err("mk_vsock: failed to allocate skb\n");
@@ -301,7 +311,6 @@ static void mk_vsock_ipi_handler(u32 msg_type, u32 subtype,
 
 	skb_put_data(skb, payload, payload_len);
 
-	hdr = (struct virtio_vsock_hdr *)skb->data;
 	mk_queue_rx_pkt(skb);
 }
 
@@ -585,7 +593,7 @@ static bool mk_transport_stream_is_active(struct vsock_sock *vsk)
 	return true;
 }
 
-static bool mk_transport_stream_allow(u32 cid, u32 port)
+static bool mk_transport_stream_allow(struct vsock_sock *vsk, u32 cid, u32 port)
 {
 	/* Allow connections to any multikernel instance */
 	return true;

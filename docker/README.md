# Docker experiment input

`daxfs-proof.Dockerfile` supplies a deterministic Docker-derived filesystem for
the DAXFS child-root experiment. Docker provides the OCI filesystem; Kerf and
Multikernel select and boot the child kernel.

See the [DAXFS report](../docs/experiments/daxfs/report.md).

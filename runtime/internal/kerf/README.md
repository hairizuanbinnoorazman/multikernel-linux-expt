# Kerf adapter

Own exact argv construction, timeouts, structured parsing, and backend error
mapping. Never accept shell fragments. Keep the adapter replaceable by a
future machine-readable Kerf API.

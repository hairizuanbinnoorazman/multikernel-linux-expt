# Protocols

This directory will contain schemas for:

- daemon client and event APIs;
- daemon-to-agent process lifecycle;
- authenticated sandbox identity and generation; and
- storage and network control messages.

Schemas require explicit versions, bounded message sizes, compatibility rules,
and generated-code tests before network-facing implementations are accepted.
